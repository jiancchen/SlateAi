# Shared Model Utilities

This folder is for reusable model primitives shared across cartridges.

Use it for small, stable helpers such as calibration math, metric formatting, manifest helpers, and cross-sport validation utilities.

Do not put sport-specific scoring logic here. Sport-specific behavior belongs inside `models/{sport}/cartridges/{modelId}/`.

