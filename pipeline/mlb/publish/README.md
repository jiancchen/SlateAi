# MLB Publish

MLB public artifact exporter compatibility launchers live here after migration.

The canonical MLB-M0 lane implementations live in `models/mlb/cartridges/MLB-M0/lanes/`.
These files preserve older direct `pipeline/mlb/publish/*` calls while the migration finishes.
